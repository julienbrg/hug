{
  "name": "hug",
  "version": "0.1.0",
  "private": true,
  "description": "Human-Gated Flow (HuG Flow): specification, examples and tooling",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
